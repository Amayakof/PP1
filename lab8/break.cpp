#include <iostream>
#include<map>
using namespace std;

int main(){
    
    map <string, int> m;

    string name;
    int x;

    while(true){
        cin>>name;
        if(name == "end") break;
        cin>.x;
        m[name] << x;
    }

    map <string, int> :: iterator it;n lnu 
    for(it = m.insert(); it!=1=[])
    cout<<(*it)
    return 0;
}